# Orchestration Log: Trinity — 2026-04-02T1719

**Agent:** Trinity (ML Engineer)  
**Session:** 1.7B Implementation (Phase 1 Python Export Scripts)  
**Mode:** background  
**Status:** ✅ Success

## Summary

Refactored all Python ONNX export scripts to be config-driven, replacing hardcoded 0.6B constants. Added 1.7B model support with new download options. Backward-compatible with 0.6B defaults.

## What Was Done

### Export Script Refactoring

- **export_lm.py** — Removed hardcoded `TALKER_HIDDEN=1024`, `CP_HIDDEN=1024`, etc. Added `read_model_dims(config)` function extracting dimensions from `config.talker_config` and `config.code_predictor_config`. Wrapper classes now accept `num_layers` as constructor argument
- **reexport_lm_novmap.py** — Same config-driven refactor. Added `argparse` CLI with `--model-dir` and `--output-dir` options (previously hardcoded paths)
- **reexport_base_novmap.py** — Same config-driven refactor. Added `argparse` CLI
- **download_models.py** — Extended with 1.7B variants. New `--model` choices: `customvoice-1.7b`, `base-1.7b`, `all-1.7b`, `everything` (in addition to existing 0.6B options)
- **extract_tokenizer.py** — Added `--model` CLI argument for flexibility (tokenizer is shared across variants)
- **export_embeddings.py** — Already config-driven; updated docstring to document 1.7B support
- **Documentation** — Updated `python/README.md` with comprehensive 1.7B export instructions, GPU requirements (16GB RAM), and time estimates. Updated `python/ARCHITECTURE.md` with 0.6B vs 1.7B comparison table

### Key Pattern

```python
def read_model_dims(config):
    tc = config.talker_config
    cp = tc.code_predictor_config
    return {
        "talker_num_layers": tc.num_hidden_layers,
        "talker_hidden": tc.hidden_size,
        "cp_num_layers": cp.num_hidden_layers,
        "cp_hidden": cp.hidden_size,
        ...
    }
```

This pattern should be reused in any new export scripts.

### Files Unchanged

- `export_vocoder.py` — Vocoder architecture independent of Talker hidden size
- `export_speaker_encoder.py` — Already reads config dynamically
- `validate_vocoder.py` — Vocoder-only validation
- `patch_models_for_dml.py` — Auto-detects dimensions from ONNX graph

## Backward Compatibility

All scripts retain 0.6B as default. Running without arguments produces identical behavior to before this session.

## Impact on Teammates

- **Neo (.NET):** config.json already includes all dynamic dimensions via `export_embeddings.py`. No C# changes needed to config format; Neo handles integration
- **Tank (QA):** No new test files from Trinity (tests created by Tank); backward compat verified

## Outcome

✅ All Python export scripts config-driven. 0.6B backward compatible. Ready for GPU-based 1.7B export when approved.
