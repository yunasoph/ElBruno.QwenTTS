# Orchestration Log: Morpheus (Lead / Architect)

**Date:** 2026-04-02T16:43Z  
**Agent:** Morpheus (Lead / Architect)  
**Mode:** Background  
**Task:** Assess scope and codebase impact of supporting Qwen3-TTS 1.7B model variant

## Outcome: MEDIUM EFFORT, NON-BREAKING, RECOMMENDED

✅ **Recommendation:** PURSUE Phase 1 support.

## Codebase Impact Assessment

### C# Changes Required

1. **LanguageModel.cs** (Priority: HIGH)
   - Currently hardcodes `1024` buffer sizes in 6 locations (lines 101, 114, 136-179)
   - Refactor: Replace with `cfg.talker.hidden_size` dynamically
   - ~40 references total

2. **EmbeddingStore.cs** (Priority: MEDIUM)
   - Add `HiddenSize` property to config, read from `config.json` at runtime
   - Make `TextProjection()`, `TalkerCodecEmbedding()`, `CpCodecEmbedding()` dimension-aware
   - ~66 references to dimension constants

3. **ModelDownloader.cs** (Priority: HIGH)
   - Add model selection mechanism: enum-based `QwenModelVariant`
   - New factory overload: `CreateAsync(variant, ...)`
   - Maintain backward compatibility (default: CustomVoice_0.6B)

4. **TtsPipeline.cs, Vocoder.cs, other** (Priority: NONE)
   - No changes needed — already config-driven or dimension-agnostic

### Python Changes Required

- **export_lm.py**: Remove hardcoded `TALKER_HIDDEN = 1024`, read from `model.config.talker_config.hidden_size`
- **export_embeddings.py**: Already dynamic ✅
- **download_models.py**: Add 1.7B model variant options

## Scope Summary

- **Effort:** Medium (1-2 days)
- **Risk:** Low — non-breaking, existing 0.6B users unaffected
- **Memory:** 1.7B requires ~12 GB disk, 16 GB RAM (document clearly)
- **User benefit:** Instruction control (emotion, rate, timbre), adaptive prosody

## Phased Approach (Recommended)

**Phase 1 (Issue #26):**
1. Refactor LanguageModel.cs for dimension-agnostic buffer allocation
2. Add QwenModelVariant enum to ModelDownloader
3. Export 1.7B ONNX models via Python
4. Upload to HuggingFace repo (new: `elbruno/Qwen3-TTS-12Hz-1.7B-CustomVoice-ONNX`)
5. Add unit tests for 1.7B config (mock-based)
6. Update README + docs with model comparison table

**Phase 2 (Future):** FP16 export, quantization, optimizations

---

**Proposal document:** `.squad/decisions/inbox/morpheus-17b-scope.md`
