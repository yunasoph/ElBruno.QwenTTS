# Orchestration Log: Tank — 2026-04-02T1719

**Agent:** Tank (Tester/QA)  
**Session:** 1.7B Implementation (Phase 1 Test Coverage)  
**Mode:** background  
**Status:** ✅ Success

## Summary

Created 47 new xUnit tests for multi-variant model support across 3 new test files. All 88 tests pass (78 Core + 10 VoiceCloning). TDD-first approach — types and tests written before Neo's integration work.

## What Was Done

### New Test Files

- **ModelVariantTests.cs** (29 tests)
  - `QwenModelVariant` enum values and defaults
  - `QwenModelVariantConfig` dimension mappings (hidden_size, intermediate_size for 0.6B and 1.7B)
  - HuggingFace repo ID mappings
  - Model subdirectory resolution
  - Structural invariant: intermediate_size == 3× hidden_size
  - Invalid variant handling (ArgumentOutOfRangeException)

- **ModelVariantDownloaderTests.cs** (10 tests)
  - Download directory isolation per variant
  - 0.6B legacy directory (backward compat)
  - 1.7B subdirectory isolation
  - Variant-aware directory resolution
  - Concurrent variant downloads don't collide

- **TtsPipelineVariantTests.cs** (8 tests)
  - `TtsPipeline.CreateAsync()` backward compatibility (no variant specified → 0.6B)
  - Optional variant parameter works with both 0.6B and 1.7B
  - Variant propagates through to ModelDownloader and EmbeddingStore
  - Config.json read correctly for each variant

### Test Quality

- **Naming:** Consistent AAA pattern (Arrange-Act-Assert)
- **Assertions:** Strong use of xUnit (Assert.Equal, Assert.Throws, Assert.Single)
- **Coverage:** Enum contracts, config mappings, directory isolation, backward compat
- **Edge Cases:** Default enum value, invalid variants, variant switching in same session

## Key Design Decisions (Tested)

- `QwenModelVariant.Qwen06B = 0` ensures `default(QwenModelVariant)` maps to 0.6B (backward compat tested)
- Variant directories are subdirs under shared cache root (tested)
- Config lookups use switch expressions with proper exception handling (tested)
- intermediate_size is always 3× hidden_size (structural invariant tested)

## Impact on Teammates

- **Neo (.NET):** All test contracts ready; Neo integrates types into ModelDownloader, TtsPipeline, EmbeddingStore
- **Trinity (ML):** No test impact; config.json format already handled by export scripts

## Test Results Summary

```
Total tests: 88 pass
├── Core tests: 78
│   ├── ModelVariantTests: 29
│   ├── ModelVariantDownloaderTests: 10
│   ├── TtsPipelineVariantTests: 8
│   └── Existing tests: 31
└── VoiceCloning tests: 10

Build: Clean, no warnings
```

## Outcome

✅ All 88 tests pass. Complete test coverage for variant config API. Ready for production use.
